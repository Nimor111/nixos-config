{ pkgs, ... }:

{
  # nix-prefetch-git <git-url>
  # plugin-name = pkgs.vimUtils.buildVimPlugin {
    # name = "plugin-name";
    # src = pkgs.fetchFromGitHub {
      # owner = "plugin-org";
      # repo = "plugin-repo";
      # rev = "<ref>";
      # sha256 = "<sha256>";
    # };
  # };
}
